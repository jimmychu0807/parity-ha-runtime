chain:
  + A can create a cat (id, name, price)
    + can you store a name?
    + make user storing kitties in an array
  * A set a cat in auction
  * B can purchase a cat from A after paying A price

chain-ui:
  * show # of cats available for sold
  * a button that B can purchase it
    L how do you represent a user in the browser?
